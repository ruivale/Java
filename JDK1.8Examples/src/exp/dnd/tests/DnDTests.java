package exp.dnd.tests;

import javax.swing.*;

/**
 * <p>Title: </p>
 *
 * <p>Description: </p>
 *
 * <p>Copyright: Copyright (c) </p>
 *
 * <p>Company: </p>
 *
 * @author not attributable
 * @version 1.0
 */
public class DnDTests {
/*
  public DnDTests() {
    JFrame frame = new JFrame();
    JPanel panel = new JPanel();
// Set up drag manager
    DraggableManager manager = new DefaultDraggableManager();
    manager.setDraggableContainer(panel); // note I wired them up first.
    manager.setDragPolicy(DragPolicy.OPEN);

// Add components to be draggable
    JButton aButton = new JButton("Draggable Button");
    JLabel aLabel = new JLabel("Draggable Label");
    panel.add(aButton);
    panel.add(aLabel);

// compose frame.
    frame.getContentPane().add(panel);
    frame.setDefaultCloseOperation(JFrame.DISPOSE_ON_CLOSE);
    frame.setSize(500, 500);
    frame.setVisible(true);
  }
 */
}
/*
class DraggingButton extends JButton implements DraggableTree {
 public Component getComponent() {
  return this;
 }
}
*/

